info:
  name: Actuator | health
  type: http
  seq: 1

http:
  method: GET
  url: http://tdc-contabo.tt:8100/actuator/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
