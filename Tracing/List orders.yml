info:
  name: List orders
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8087/api/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
