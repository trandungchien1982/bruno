info:
  name: List payments
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8088/api/payments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
