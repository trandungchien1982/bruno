info:
  name: Add Order
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8087/api/orders
  body:
    type: json
    data: |-
      {
        "username": "Order 01"
        , "email": "test-order@ttt.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
