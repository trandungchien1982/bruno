info:
  name: Add user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8086/api/users
  body:
    type: json
    data: |-
      {
        "username": "Name 01"
        , "email": "test-email@ttt.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
