info:
  name: Actuator | prometheus
  type: http
  seq: 3

http:
  method: GET
  url: http://tdc-contabo.tt:8100/actuator/prometheus
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
